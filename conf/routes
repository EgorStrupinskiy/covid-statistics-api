# Routes
# This file defines all application routes (Higher priority routes first)
# https://www.playframework.com/documentation/latest/ScalaRouting
# ~~~~

# Get covid cases for a list of countries in time period
GET     /statistics                  controller.CountryStatController.getCovidData()

# Get full list of available countries
GET     /countries                  controller.CountryController.getCountries()